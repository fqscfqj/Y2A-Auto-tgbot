version: '3.8'
services:
  y2a_auto_tg_bot:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - TG_BOT_TOKEN=你的TelegramBotToken
      - Y2A_AUTO_API=http://host.docker.internal:5000
      # 如启用密码保护，取消注释并填写：
      # - Y2A_PASSWORD=你的密码
    restart: unless-stopped